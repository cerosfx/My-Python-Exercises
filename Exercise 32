users = []

if users:
	for user in users:
		if user == 'admin':
			print(f"Wow {user.title()}, Long time no see!!")
		else:
			print(f"Hello {user.title()}, nice to see you.")
else:
	print(f"This list is empty")
